import * as vscode from 'vscode';

export function activate(context: vscode.ExtensionContext) {
    console.log('DAN language extension is now active!');
    
    // Register any commands or features here
    // For now, the extension provides syntax highlighting through the grammar file
}

export function deactivate() {}

